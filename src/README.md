# WEATHER-API APP

# The Project description
```This is a project that demonstrates my grasp of asynchronous programming and the event loop. This app fetch weather data and news headlines from public API, showcasing different asynchronous styles, which are :
Callbacks, promises, and async/await.

# Installation and set-up
```bash
   typescript tsx @types/node
   package-lock.json
   package.json
   tsconfig.json
   
  
   

   Created project folder from file explore
   Clone repository:
   git clone https://github.com/khethiwengema04-gif/AsyncWeather-News.git
   npm init -y

   cd AsyncWeather-News
   ```


   # Run App
   ```bash
   npm install
   # or 
   yarn intall

   npm run dev
   npm run callback
   npm run async
   npm run promises
   ```

   # Commits
   ```terminal 
   git add .
   git commit -m ''
   git push
   ```

   # Tech Stack
   ## 1.Node
  
   